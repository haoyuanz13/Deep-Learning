The folder "cufs_std_concat" is an alternative data format which concatenates the sketches and photos in one image.      

The current dataLoader uses the converted numpy data file in "cufs_students" folder.
